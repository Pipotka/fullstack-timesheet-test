# Спецификация: Инициализация фронтенд-проекта

- **Дата**: 2026-08-24
- **Статус**: Проект
- **Этап**: Первый фронтенд-этап (инициализация)
- **Ветка**: `feat/frontend-init`

---

## 1. Цель

Инициализировать современный фронтенд-проект на базе Vite + React + TypeScript в директории `Frontend/`. На данном этапе выполняется только scaffolding и проверка сборки — без страниц, API-клиентов, бизнес-логики и UI-библиотек.

---

## 2. Область применения (Scope)

### 2.1. Входит в объём

| № | Элемент | Описание |
|---|---------|----------|
| 1 | Vite-проект | Создание проекта через `npm create vite@latest Frontend -- --template react-ts` или эквивалентную ручную конфигурацию |
| 2 | TypeScript | Строгая конфигурация (`strict: true`), `tsconfig.json` с настройками для Vite |
| 3 | React 18+ | Подключение React и React DOM как основных зависимостей |
| 4 | Зависимости | Установка зависимостей через `npm install` (или `pnpm install`) |
| 5 | Production-сборка | Проверка успешного выполнения `npm run build` |
| 6 | Корневой `.gitignore` | Создание корневого `.gitignore` с правилами для `test-task.html`, `test-task.pdf` и директории `/docs/` |
| 7 | `Frontend/.gitignore` | Создание `.gitignore` внутри `Frontend/` со стандартными правилами React/Vite |
| 8 | Базовая структура | Стандартная структура каталогов, создаваемая Vite (`src/`, `public/`, `index.html`) |

### 2.2. Не входит в объём

| № | Элемент | Обоснование |
|---|---------|-------------|
| 1 | Страницы и маршруты | Будут добавлены на последующих этапах |
| 2 | API-клиенты и HTTP-слой | Контракт API ещё не зафиксирован для фронтенда |
| 3 | Бизнес-логика | Отсутствует до определения предметных компонентов |
| 4 | UI-библиотеки (MUI, Ant Design, Tailwind и т.п.) | Выбор будет сделан на этапе дизайна UI |
| 5 | State management (Zustand, Redux) | Потребуется после появления бизнес-логики |
| 6 | Тесты (unit, e2e) | Инфраструктура тестирования будет добавлена позже |
| 7 | CI/CD конфигурация | Отдельная задача |
| 8 | Изменение существующих файлов | `NOTES.md`, `test-task.*`, `code-review/*`, `LICENSE` не изменяются |

---

## 3. Структура проекта

### 3.1. Структура репозитория после реализации

```
frontend-init/
├── .git/
├── .gitignore                    # НОВЫЙ: корневой gitignore (игнорирует test-task.*, /docs/)
├── docs/                         # УЖЕ СУЩЕСТВУЕТ: спецификации (игнорируется для новых файлов)
│   └── superpowers/
│       └── specs/
│           └── 2026-08-24-frontend-init-design.md  # Уже отслеживается Git
├── Frontend/
│   ├── .gitignore                # НОВЫЙ: gitignore фронтенда
│   ├── index.html                # Точка входа Vite
│   ├── package.json              # Зависимости и скрипты
│   ├── tsconfig.json             # Конфигурация TypeScript
│   ├── tsconfig.node.json        # Конфигурация для vite.config.ts
│   ├── vite.config.ts            # Конфигурация Vite
│   ├── public/                   # Статические ресурсы
│   │   └── vite.svg
│   ├── src/
│   │   ├── main.tsx              # Точка входа React
│   │   ├── App.tsx               # Корневой компонент
│   │   ├── App.css               # Стили корневого компонента
│   │   ├── index.css             # Глобальные стили
│   │   ├── vite-env.d.ts         # Типы Vite
│   │   └── assets/
│   │       └── react.svg
│   └── node_modules/             # Игнорируется
├── code-review/                  # БЕЗ ИЗМЕНЕНИЙ
├── NOTES.md                      # БЕЗ ИЗМЕНЕНИЙ
├── test-task.html                # Игнорируется корневым .gitignore
├── test-task.pdf                 # Игнорируется корневым .gitignore
└── LICENSE                       # БЕЗ ИЗМЕНЕНИЙ
```

### 3.2. Корневой `.gitignore`

Файл должен содержать как минимум следующие записи:

```gitignore
# Файлы тестового задания (не являются частью исходного кода)
test-task.html
test-task.pdf

# Спецификации и проектная документация (не являются частью исходного кода)
/docs/
```

Допускается добавление других общих правил (например, IDE-файлы), но `test-task.html`, `test-task.pdf` и `/docs/` должны быть указаны явно.

#### 3.2.1. Поведение Git в отношении уже отслеживаемых файлов

Правило `/docs/` в `.gitignore` применяется **только к новым неотслеживаемым файлам**. Файлы, которые уже были добавлены в индекс Git (через `git add`) и зафиксированы в истории (через `git commit`), **продолжают отслеживаться**, несмотря на наличие соответствующего правила в `.gitignore`.

**Практическое следствие для данного проекта:** файл `docs/superpowers/specs/2026-08-24-frontend-init-design.md` (данная спецификация) уже отслеживается Git. Добавление правила `/docs/` в корневой `.gitignore` **не приведёт** к прекращению отслеживания этого файла. Файл останется в индексе и будет отображаться в `git status` при внесении изменений.

Для прекращения отслеживания уже добавленного файла потребовалось бы явно выполнить `git rm --cached <file>`, однако это **не входит в объём** данной задачи (см. §2.2).

### 3.3. `Frontend/.gitignore`

Файл должен игнорировать стандартные артефакты React/Vite-проекта:

```gitignore
# Зависимости
node_modules/

# Сборка
dist/
build/

# Переменные окружения
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Логи пакетных менеджеров
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.pnpm-debug.log*

# Редакторы и IDE
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# ОС
Thumbs.db
```

---

## 4. Ограничения и правила

### 4.1. Ограничения реализации

1. **Не изменять существующие файлы.** Файлы `NOTES.md`, `test-task.html`, `test-task.pdf`, `LICENSE`, а также содержимое `code-review/` не должны быть изменены.
2. **Только инициализация.** Запрещено добавлять страницы, маршруты, API-клиенты, сторы, утилиты и бизнес-логику. Проект должен содержать только scaffolding, созданный Vite (или минимально адаптированный).
3. **Без UI-библиотек.** Запрещено устанавливать MUI, Ant Design, Chakra UI, Tailwind CSS, styled-components и прочие UI-фреймворки.
4. **Без state management.** Запрещено устанавливать Zustand, Redux, MobX, Jotai и аналоги.
5. **TypeScript strict mode.** Конфигурация `tsconfig.json` должна содержать `"strict": true`.
6. **Production-сборка.** Команда `npm run build` (или `pnpm build`) должна завершаться успешно без ошибок.

### 4.2. Правила коммитов

1. Коммит реализации должен иметь сообщение на русском языке.
2. Пример заголовка: `Спецификация инициализации фронтенда` (для данного документа) или `Инициализация Vite React TypeScript проекта` (для будущей реализации).
3. Пример описания: `Зафиксированы требования к Vite React TypeScript проекту и правилам игнорирования файлов` (для данного документа).

### 4.3. Версии и совместимость

| Компонент | Минимальная версия |
|-----------|-------------------|
| Node.js | 18.x LTS |
| Vite | 5.x |
| React | 18.x |
| TypeScript | 5.x |
| npm | 9.x (или pnpm 8.x) |

---

## 5. Критерии приёмки

### 5.1. Функциональные критерии

| № | Критерий | Метод проверки |
|---|----------|----------------|
| 1 | Директория `Frontend/` содержит валидный Vite + React + TypeScript проект | Наличие `package.json`, `vite.config.ts`, `tsconfig.json`, `src/main.tsx` |
| 2 | `npm install` завершается без ошибок | Выполнение команды в `Frontend/` |
| 3 | `npm run build` завершается успешно, создаёт `dist/` | Выполнение команды, проверка наличия `dist/index.html` |
| 4 | `npm run dev` запускает dev-сервер | Ручная проверка или `curl localhost:5173` |
| 5 | Корневой `.gitignore` содержит `test-task.html`, `test-task.pdf` и `/docs/` | Чтение файла |
| 6 | `Frontend/.gitignore` содержит все обязательные правила | Чтение файла, проверка по списку из §3.3 |
| 7 | `test-task.html` и `test-task.pdf` не отображаются в `git status` | Выполнение `git status` |
| 7a | Новые файлы в `docs/` не отображаются в `git status` как untracked | Создание тестового файла `docs/test-ignore.tmp`, выполнение `git status` — файл не должен появиться; удаление тестового файла |
| 7b | Уже отслеживаемый файл спецификации остаётся в индексе после добавления `/docs/` в `.gitignore` | Выполнение `git ls-files docs/superpowers/specs/2026-08-24-frontend-init-design.md` — файл должен быть в списке; `git status` должен показывать его при изменении |
| 8 | Существующие файлы не изменены | `git diff NOTES.md test-task.html test-task.pdf LICENSE code-review/` — пустой вывод |
| 9 | TypeScript работает в strict mode | Проверка `tsconfig.json`: `"strict": true` |
| 10 | Не установлены UI-библиотеки и state management | Проверка `package.json`: отсутствуют mui, antd, tailwind, zustand, redux и т.п. |

### 5.2. Нефункциональные критерии

| № | Критерий |
|---|----------|
| 1 | Коммит реализации содержит сообщение на русском языке |
| 2 | Коммит содержит только файлы спецификации (для данного коммита) или только файлы инициализации (для будущего коммита) |
| 3 | Спецификация не содержит TODO, TBD, незаполненных разделов |

---

## 6. План проверки

### 6.1. Автоматизированная проверка

```bash
# 1. Проверка структуры
ls Frontend/package.json Frontend/vite.config.ts Frontend/tsconfig.json Frontend/src/main.tsx

# 2. Установка зависимостей
cd Frontend && npm install

# 3. Production-сборка
npm run build
ls dist/index.html

# 4. Проверка strict mode
grep '"strict"' tsconfig.json

# 5. Проверка .gitignore
grep 'test-task.html' ../.gitignore
grep 'test-task.pdf' ../.gitignore
grep '/docs/' ../.gitignore
grep 'node_modules' .gitignore
grep 'dist' .gitignore
grep '.env' .gitignore

# 6. Проверка отсутствия изменений существующих файлов
git diff NOTES.md test-task.html test-task.pdf LICENSE code-review/

# 7. Проверка игнорирования test-task файлов
git status | grep test-task  # Не должно быть в untracked

# 8. Проверка игнорирования новых файлов в docs/
touch ../docs/test-ignore.tmp
git status | grep test-ignore  # Не должно быть в untracked
rm ../docs/test-ignore.tmp

# 9. Проверка, что спецификация продолжает отслеживаться
git ls-files ../docs/superpowers/specs/2026-08-24-frontend-init-design.md  # Должен вывести путь
```

### 6.2. Ручная проверка

1. Открыть `Frontend/.gitignore` и сверить с перечнем из §3.3.
2. Открыть корневой `.gitignore` и убедиться, что `test-task.html`, `test-task.pdf` и `/docs/` указаны явно.
3. Выполнить `npm run dev` и убедиться, что dev-сервер запускается и отдаёт страницу.
4. Проверить `package.json` — убедиться, что нет лишних зависимостей.
5. Проверить сообщение коммита через `git log -1`.

---

## 7. Зависимости и риски

### 7.1. Зависимости

| Зависимость | Влияние |
|-------------|---------|
| Node.js 18+ | Необходим для Vite 5.x и современного TypeScript |
| Доступ к npm registry | Требуется для `npm install` |
| Свободная ветка `feat/frontend-init` | Коммиты выполняются в эту ветку |

### 7.2. Риски

| Риск | Вероятность | Митигация |
|------|-------------|-----------|
| Конфликт с изменёнными файлами в рабочей директории | Средняя | Коммит спецификации включает только файл спецификации; остальные изменения не stage-ятся |
| Несовместимость версий Node.js и Vite | Низкая | Указать минимальные версии в спецификации |
| Платформенные различия (Windows/Linux) | Низкая | Vite кроссплатформенен; пути в `.gitignore` используют `/` |

---

## 8. Глоссарий

| Термин | Определение |
|--------|-------------|
| Scaffolding | Начальная структура проекта, создаваемая инструментом инициализации |
| Strict mode | Режим TypeScript с максимальной строгостью проверки типов |
| Production-сборка | Компиляция проекта в оптимизированные статические файлы для развёртывания |
| Dev-сервер | Локальный сервер разработки с hot module replacement |

---

## 9. История изменений

| Дата | Автор | Изменение |
|------|-------|-----------|
| 2026-08-24 | — | Первоначальная версия спецификации |
| 2026-08-24 | — | Добавлено требование игнорирования `/docs/` в корневом `.gitignore`; описано поведение Git для уже отслеживаемых файлов (§3.2.1); обновлены критерии приёмки и проверки |
